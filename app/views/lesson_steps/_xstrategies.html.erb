<% if Xstrategy.where(:standard_id => standard.id).empty? %>
  No preset strategies exist for this standard. Continue to the next page to add them.
  <% else %>

  
  <%= f.association :xstrategies, collection: Xstrategy.where(:standard_id => standard.id).order("standard_id DESC"), as: :check_boxes, :label_method => lambda { |strategy| "#{strategy.content}" }, input_html: {:data => {:toggle => "checkbox"}}, label: false %>
  
  <% end %>