<% if signed_in? %>

<div class="page-header">
  <h1>Lesson Setup</h1>
</div>


<%= simple_form_for(@select, :html => { :class => 'form-horizontal' }) do |f| %>

    <%= f.input :name, input_html: {style: 'width:100%;'}, label: "Lesson name:", autofocus: true %><br />
    <%= f.input :description, as: :text, input_html: {style: 'width:100%; height:50px'}, label: "Description:" %><br />
    <%= f.input :objective, input_html: {style: 'width:100%; height:50px'}, label: "Objective:" %><br />
   <br />
   <div class="row">
  <div class="col-md-2"><%= f.association :grades, label: "Grade(s):", as: :check_boxes, label_method: :number %><br /><br /></div>
  <div class="col-md-3"><%= f.association :subject, as: :radio_buttons, label_method: :name, label: "Reading Subject:" %><br /></div>
<div class="col-md-3"><%= f.association :subsubjects, label: "Subject(s):", as: :check_boxes, label_method: :name %><br /><br /></div>
<div class="col-md-4"><%= f.association :sources, label: "Source(s):", as: :check_boxes, label_method: :name %><br /><br /></div>
</div>

        
    
  <div class="form-actions">
 
 <button type="submit" class="btn btn-primary" data-loading-text="Saving..">Save and Continue</button>
  </div>
<% end %>

	
	<% else %>
	<center>Please sign in before continuing.<br /><%= link_to "Sign in", signin_path, class: "btn btn-link" %>
		<% end %>


<script>
	$('button[data-loading-text]').click(function () {
    $(this).button('loading');
});
</script>
