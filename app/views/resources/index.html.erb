<h1>Resources</h1>

<table>


<% @resources.each do |resource| %>
  <tr>
    <td><%= resource.title %></td></tr>
    <tr><td><%= resource.content %></td></tr>
   <tr> <td><%= simple_format(resource.media) %></td></tr>
  <tr>
    <td><%= link_to 'Show', resource %></td>

  </tr>
<% end %>
</table>

<br />
<% if current_user && current_user.admin? %>
Admin: 
<%= link_to 'New Resource', new_resource_path %>
<% end %>