<h1>Listing xlinks</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @xlinks.each do |xlink| %>
  <tr>
    <td><%= link_to 'Show', xlink %></td>
    <td><%= link_to 'Edit', edit_xlink_path(xlink) %></td>
    <td><%= link_to 'Destroy', xlink, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Xlink', new_xlink_path %>
