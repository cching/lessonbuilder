<%= simple_form_for(@cskill) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :select_id %>
    <%= f.input :standard_id %>
    <%= f.input :content %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
