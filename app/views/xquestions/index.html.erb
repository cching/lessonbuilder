<h1>Listing xquestions</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @xquestions.each do |xquestion| %>
  <tr>
    <td><%= link_to 'Show', xquestion %></td>
    <td><%= link_to 'Edit', edit_xquestion_path(xquestion) %></td>
    <td><%= link_to 'Destroy', xquestion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Xquestion', new_xquestion_path %>
