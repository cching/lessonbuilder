<% if current_user.admin? %>
<h1>Users</h1><br />

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
	 <td><%= user.name %></td>
	 <td><%= user.email %></td>
	 <td><%= user.created_at %></td>
    <td><%= link_to 'Delete', user, method: :delete, class: "btn btn-danger btn-mini", data: { confirm: 'Are you sure you want to delete this user?' } %></td>
  </tr>
<% end %>
</table>


<% end %>
